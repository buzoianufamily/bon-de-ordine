# Date specifice instantei — NU se urca in git (contin secrete / fisiere incarcate)
/config/tenants.json
/assets/uploads/*
!/assets/uploads/.gitkeep
# Backup-uri ale bazei de date (contin datele clientilor) — niciodata in git
/backups/
